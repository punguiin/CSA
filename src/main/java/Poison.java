public final class Poison {
    public static final byte[] BYTES = new byte[0];
    public static final Packet PACKET = new Packet((byte) 0x13, (byte) 0, 0L, 0, new Message(0, 0, new byte[0]));

    private Poison() {}
}
